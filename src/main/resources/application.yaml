spring:
  application:
    name: boki-spring-actuator1
management:
  endpoints: # 노출
    web:
      exposure:
        include: "*"
  endpoint: # 활성화
    health:
#      show-components: always
      show-details: always
  info:
    env:
      enabled: true
    git:
      enabled: true
    build:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
